# /acli-check

Verify acli installation and authentication status.

## Command
```bash
acli --version && acli jira auth status
```

Expected output:
- acli version (e.g., `acli version 1.3.14-stable`)
- Authentication status (`Authenticated` with site and email)
